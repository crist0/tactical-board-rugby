# Ball Components

## Purpose
Contains components for displaying and interacting with the rugby ball element.

## Planned Components
- `BallToken.vue` (Phase 3) - Draggable ball marker.
- `BallTrail.vue` (Phase 6) - Optional motion trail during animation playback.

## Usage Notes
- Ball position will be synchronized with play keyframes in play-related stores.
